Level 1 — from the project folder (no setup):
pbpaste | npm run -s clean | pbcopy
Copy something, run that, and your clipboard now holds the cleaned version. Add -- --explain to see why each block was treated the way it was.
