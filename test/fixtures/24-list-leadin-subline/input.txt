1. Install the dependencies with npm install
2. Set these two env vars before the first run:
   CLEANCOPY_HOME and CLEANCOPY_LOG_LEVEL
3. Start the watcher and copy something from the
   terminal to try it out
